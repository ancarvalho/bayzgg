export function CalculateAproxTime(time: string) {
  const timeBefore = new Date(time).toLocaleDateString("pt-br")
  const timeNow = new Date().toISOString()
  
  // const diff = timeNow - timeBefore
}